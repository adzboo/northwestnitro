# Server-only PostgreSQL connection; use a pooled connection for serverless hosting.
DATABASE_URL=
# Supabase project URL and publishable/anon key (never a service role key).
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Exact website origin, no trailing slash.
SITE_URL=http://localhost:5174
# Optional; online payments stay disabled until configured.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
