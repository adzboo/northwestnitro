 'use client';
import {useState} from 'react';
export default function Login(){const [email,setEmail]=useState(''),[token,setToken]=useState(''),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');return <form className="panel stack" style={{maxWidth:520}} onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:sent?'verify':'send',email,token,returnTo:new URLSearchParams(location.search).get('return_to')})});const d=await r.json();if(!r.ok)throw new Error(d.error);if(d.returnTo)location.assign(d.returnTo);else setSent(true);}catch(e){setError(e instanceof Error?e.message:'Please try again.');}finally{setBusy(false);}}}>{error&&<p className="notice error" role="alert">{error}</p>}<label className="field">Email address<input type="email" autoComplete="email" required maxLength={254} value={email} readOnly={sent} onChange={e=>setEmail(e.target.value)}/></label>{sent&&<><p role="status">Check your email for your code.</p><label className="field">Sign-in code<input inputMode="numeric" autoComplete="one-time-code" required pattern="[0-9]{6,10}" value={token} onChange={e=>setToken(e.target.value)}/></label></>}<button className="button lime" disabled={busy}>{busy?'Please wait...':sent?'Sign in':'Email me a code'}</button>{sent&&<button type="button" className="text-link" disabled={busy} onClick={()=>{setSent(false);setToken('');setError('');}}>Change email or request another code</button>}<p className="fine">New to the club? Sign in first, then complete your membership application. Membership approval is handled by the club.</p></form>}
